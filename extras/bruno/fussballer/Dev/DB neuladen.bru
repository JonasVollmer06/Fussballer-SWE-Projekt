meta {
  name: DB neuladen
  type: http
  seq: 1
}

post {
  url: {{devUrl}}/db_populate
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
  res.headers['content-type']: eq application\/json
  res.body.db_populate: eq success
}

settings {
  encodeUrl: true
  timeout: 0
}
