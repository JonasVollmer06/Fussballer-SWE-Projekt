meta {
  name: Suche mit vorhandener ID (falsche Versionsnummer)
  type: http
  seq: 3
}

get {
  url: {{restUrl}}/:id
  body: none
  auth: inherit
}

params:path {
  id: 30
}

headers {
  Accept: application/json
  If-None-Match: xxx
}

assert {
  res.status: eq 200
  res.headers['content-type']: eq application/json
  res.body.id: eq 3
}

settings {
  encodeUrl: true
  timeout: 0
}
