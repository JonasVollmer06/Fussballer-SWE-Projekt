meta {
  name: Token mit falschem Passwort anfordern
  type: http
  seq: 2
}

post {
  url: {{tokenUrl}}
  body: json
  auth: inherit
}

body:json {
  {
    "username": "admin",
    "password": "TEST"
  }
}

assert {
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
